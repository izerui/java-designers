App.main(args) {
    // 构造一个消息
	var messenger = new Messenger();
	// 打印消息
	messenger.messageFromOrcs()msg.print() {
	    // 前置
		printThisBefore() {
		}
		// 打印当前消息内容
		children.forEach(LetterComposite::print);
		// 后置
		printThisAfter() {
		}
	}
}
